apiVersion: 1

providers:
  - name: "Default"
    orgId: 1
    folder: "Event Data Aggregation Analysis" # 그라파나 UI에 표시될 폴더 이름
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards # 컨테이너 내부 경로
